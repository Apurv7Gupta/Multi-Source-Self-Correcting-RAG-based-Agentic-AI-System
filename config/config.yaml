models:
  - type: main
    engine: llm # placeholder — ignored since llm= is passed directly
    model: meta-llama/Llama-3.1-8B-Instruct

rails:
  # input:
  #   flows:
  #     - self check input
  # output:
  #   flows:
  #     - self check output
      # - self check facts
